# C Math Functions

C provides mathematical functions through the `math.h` header file.
#include <math.h>
sqrt() → square root
pow() → power
ceil() → upward rounding
floor() → downward rounding
round() → nearest integer
abs() → absolute distance from 0
sin(), cos(), and tan()